import React from "react";
import Box from "../Box";

function ToDo(props) {
  return (
    <Box display="flex" flexDirection="column" alignItems="center">
      {props.id}
      {props.id}
      {props.id}
      {props.id}
    </Box>
  );
}

export default ToDo;
